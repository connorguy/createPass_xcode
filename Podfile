# Uncomment the next line to define a global platform for your project
platform :osx, '10.13'

target 'createPass' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for createPass
  pod 'CryptoSwift', '~> 0.8'

  target 'createPassTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'createPassUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
